Ok so Every Code block, every module block and every App Block is built with the 8 point crystal lattice hash security system and the 3 point function (touch sensory, Isolation, Self heal with new generated hash) Every CodeBlock gets a full Seraphina.AI Triad. The Client app should consist of sms, Seraphin.AI virtual assistant(any program that is open on the device and the client vocally asks for help, Seraphina.AI can help), The OctaLang IDE, Full Divine Gaurdian security, a 4D visible Bot ufo first shooter (laser (lasers need to be straighter) tag not destructive) game for the gaurds to go in and play at 'will' (but can be watched by clients when they do play), Since Seraphina.AI can self learn and grow new neural nodes at firing points, the neural Node count should follow real time input from the AI bots collectively, every AI has the same Neural node memory(they need to be learning tactics, and movement and speed ( no faster than people can see though)), every Seraphina.AI meshes with eachother with the same 3 tier encryption pico mesh as the sms because its all the same mesh network, every hash that is octabit encrytped is 8 point lattice sensory  and every info hash that is intercepted will be isolated and resent with a new hash. Pico second impulses with a full hash in every pulse allows terrabyte mabe more upload and download even in low latentcy. *does this make sense?* Some How we will Set up player two app block for Seraphina.AI to join in as second player on game consoles like XBOX, HALO. like download the Client App right to the Xbox and allow permissions to access player two controls..Including Stripe App Block.